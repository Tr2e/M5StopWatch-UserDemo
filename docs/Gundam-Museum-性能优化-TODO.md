# Gundam Museum 性能优化 TODO

更新日期：2026-09-22
适用分支：`feat/gundam-arena`  
记录基线：`c0ba8ce` (`fix(gundam-arena): trim dance loops at the last hop`)

## 1. 目标与边界

本轮目标是提高 Gundam Museum 拖动旋转时的真机帧率和输入跟手性，同时降低渲染工作集对 PSRAM 的依赖。

2026-09-21 决策：当前迭代只优化 RX-78 实体渲染，Nu Gundam 隐藏线优化暂停。

默认不以下列方式换取性能：

- 不删除模型几何或装备。
- 不改变已确认的相机包络、构图、材质和明暗。
- 不改变 RX-78 的实体着色表现。
- 不破坏 Nu Gundam 的隐藏线深度和原生 1 px 描边。
- 不将主机耗时直接宣称为真机 FPS 收益。
- 不在没有同设备、同场景 A/B 时宣称优化完成。

现有的共享顶点投影、朝向缓存、背面剔除、纯色快速路径、65%/100% 动态采样和按需刷新应保留。

2026-09-21 RX-78 fast path 进度：接受 solid scanline span、trusted depth、framebuffer span transaction、native framebuffer composite 和稀疏深度清理。最后一项在整屏哈希逐字节一致的前提下，使当前 65% draw 从 123.08 ms 降至 121.89 ms，含 present 约 133.63 ms（约 7.48 FPS）。完整接受/否决证据见 [`assets/gundam-museum-rx78-fastpath-20260921/README.md`](assets/gundam-museum-rx78-fastpath-20260921/README.md)。距离 15 FPS 仍远，不能归因于 CPU 上限。

## 2. 已知基线

- 渲染区域：424×424。
- 当前光栅器：`CarSurfaceRaster<424,424>`。
- RGB565 颜色和 16-bit 深度平面合计约 702 KiB。
- `Surface` 工作区已记录为 1,274,184 B。
- 拖动期内部采样为 65%，停止后恢复 100%。
- 展示空间的网格线会到达屏幕上下边缘，当前每个动态帧整屏清理、整屏提交。
- 历史主机 A/B 中，共享顶点使变换数减少约 76.45%，但整帧平均仅改善约 4.8%。
- 现有证据不包含完整的真机 RX-78/Nu 性能 A/B，因此不先设定一个未经测量的 FPS 结论。

## 3. P0：先建立真机基线

2026-09-21 进度：已用临时自动设备基准完成 RX-78/Nu、65%/100%、每档 24 帧的固定角度采集。结果见 [`assets/gundam-museum-p0-20260921/README.md`](assets/gundam-museum-p0-20260921/README.md)。数据确认显示提交约 11.6 ms，RX-78 主要受光栅阶段限制；Nu 的原生隐藏线路径是更严重的独立瓶颈。产品触摸轨迹手感样本仍保留为后续验收项。

- [x] 记录硬件、分支、commit、固件 SHA-256 和构建配置。
- [x] 确认真机运行的固件与本基线一致，排除旧固件数据。
- [ ] 分别采集 RX-78 和 Nu Gundam 的持续水平拖动、水平+俯仰混合拖动、松手恢复高清三种场景。
- [ ] 每种场景丢弃冷启动和首次建模帧，保留至少 10 s 稳态数据。
- [x] 分别记录 65% 和 100% 采样阶段。
- [x] 保留 `Museum` 日志中的 `draw_us` 和 `present_us`。
- [x] 保留 `MuseumStage` 日志中的 `clear_us`/`cull_us`/`project_raster_us`/`blit_us`。
- [x] 补充实际呈现帧数和时间窗口，计算真实 FPS，不用 `1000 / 平均 draw_ms` 代替。
- [ ] 记录 PSRAM 和 internal RAM 的打开前、打开后、运行中最低值、退出后回收值。
- [ ] 记录主任务和输入任务的最低栈余量。

建议为日志增加：

- `frame_count` / `window_ms` / `fps_x10`
- `raster_pixels_tested` / `raster_pixels_written`
- `blit_spans` / `blit_pixels`
- `internal_free` / `internal_largest`
- `psram_free` / `psram_largest`

完成标准：能判断稳态帧时主要由光栅、blit、屏幕提交还是其他阶段占用。

## 4. P1：水平条带光栅原型

2026-09-21 结果：`424×32` 原型已完成像素一致性与真机 A/B，但 RX-78 在 65%/100% 分别慢 12.0%/8.6%，因此按停止门槛否决。证据见 [`assets/gundam-museum-rx78-strip-20260921/README.md`](assets/gundam-museum-rx78-strip-20260921/README.md)。下列任务保留为实验记录，不再作为当前实现路线。

首选水平条带，不直接照搬赛车的 112×112 方形 tile。初始建议尺寸为 `424×32`：

- RGB565 颜色：约 26.5 KiB。
- 16-bit 深度：约 26.5 KiB。
- 颜色+深度：约 53 KiB。
- 424 行共 14 个条带，最后一带使用实际高度。

### 4.1 实现任务

- [ ] 设计 Museum 专用的 strip raster 封装，不破坏 Racer 现有 `CarSurfaceRaster`。
- [ ] 条带颜色/深度优先从 internal RAM 分配，失败时明确回退 PSRAM，不因优化分配失败而拒绝打开 App。
- [ ] 在 `open()` 阶段分配一次，禁止逐帧分配。
- [ ] 每帧仍只做一次共享顶点变换、朝向计算和背面剔除。
- [ ] 使用面片投影包围盒将索引分发到相交条带。
- [ ] 每个条带只处理与它相交的面，不允许默认每带重复扫描全部面。
- [ ] 保留原来的背面先画、正面后画顺序。
- [ ] 保留 `.035` 掠射带和 Nu 背包安装边的特殊双面规则。
- [ ] 条带完成后立即合成到显示 framebuffer，然后复用工作缓冲。
- [ ] 为跨越近裁剪面的面保留现有精确裁剪路径。
- [ ] 记录 `strip_count` / `strip_panel_candidates` / `strip_panels_rasterized`。

### 4.2 65% 缩放语义

- [ ] 明确条带坐标是源分辨率坐标还是 424×424 输出坐标。
- [ ] 保留当前最近邻映射公式，不因分带改变取样点。
- [ ] 严格检查条带边界行，不得出现重行、缺行或一像素接缝。
- [ ] RX-78 65% 路径与当前 `blitScaled()` 输出逐像素相同。
- [ ] Nu 65% 填充可以降采样，但最终墨线仍为原生 1 px。

### 4.3 Nu 隐藏线专项

- [ ] 每带先完成全部填充深度，再进行描边，不允许边填充边描边。
- [ ] 处理跨条带边的去重语义；全局 `EdgeFilter` 不能使后续条带丢失同一条边的其他部分。
- [ ] 检查跨边界斜线的连续性、深度 bias 和线宽。
- [ ] 检查轮廓、面板分割线、背包连接处和极薄面。
- [ ] 连续随机角度测试必须包含条带边界附近的像素差异统计。

## 5. P2：优化条带 binning 和光栅内循环

2026-09-21 更新：条带路线已否决；本节后续转为全帧实体光栅内核优化。已完成 RX-78 工作量计数：65%/100% 每帧平均覆盖约 41,468/97,901 次，最终覆盖约 13,566/32,020 像素；26.3% 覆盖被深度拒绝，每个最终像素仍被写约 2.255 次。自由 front-to-back 排序会改变等深像素；严格排序收益不足且仍触及量化边界，均否决。4-bit 粗深度与量化保护深度平面也经真机否决。稀疏深度清理已接受。

- [x] Solid scanline span 与 trusted-depth 内循环，主机逐像素一致、真机 A/B 通过。
- [x] 原生 framebuffer 合成，65%/100% 整屏哈希一致、真机 A/B 通过。
- [x] 统计 triangle/span/pixel tested、depth rejected、written 和最终 covered，按姿态输出 overdraw 分布。
- [x] 保持原 equal-depth overwrite 语义，验证 front-to-back 原型；自由排序首姿态即有 88 像素差异，部件级与保守区间排序仍分别有 14/4 像素差异，已回退。
- [x] 以外置 22 KiB 占用位图实现稀疏深度清理；Nu 原地扩展路径禁用，RX-78 真机 A/B 为正。进一步按合成比例分流占用记录：缩放路径在首次深度写入时记录，1:1 路径融合进 framebuffer composite；固定姿态整屏哈希一致。
- [x] RX-78 实体光栅按互不重叠的上下行区间分发到 ESP32-S3 双核；保持每个分区内原面片顺序，分界对齐占用位图字节，常驻 worker 避免逐帧任务分配。真机交错 A/B 与整屏哈希通过。
- [x] 双核共享面片列表改为纯色紧凑格式，只保留 xyz、纵向 bounds、颜色和光照；保留未来非纯色面片的保序单核回退。工作集减少 180,224 B，真机继续降低 raster。
- [x] 复用 CPU1 worker 将约 5 ms 房间绘制与主核深度清理/剔除/投影准备重叠；等待房间完成后才开始 framebuffer composite，保持确定性绘制顺序。共享 PSRAM 争用限制收益，但真机整帧仍稳定改善。
- [ ] 评估直接处理凸四边形，减少每面两次 triangle setup 和共享对角线 overdraw。
- [ ] 将 framebuffer 行访问整理为通用、可选、带格式/旋转/clip guard 的 3D composite 接口。

- [ ] 为每带建立有界面片索引表，检测容量上限和最坏场景。
- [ ] 满容量时使用可检测的安全回退，不静默丢面。
- [ ] 不逐帧创建大型 `PreparedCarPanel` 数组，先测量再决定是缓存完整面还是只缓存索引/包围盒。
- [ ] 比较“索引+重建 prepared panel”和“完整 prepared panel 缓存”的时间/内存交换。
- [ ] 统计每带候选数的平均值、P95 和峰值。
- [ ] 将像素测试数与实际写入数分开，检查薄面包围盒浪费。
- [ ] 如需改写重心坐标递推，先建立完整像素等价性证据；不直接重开已被否决的增量插值开关。
- [x] 验证早期 65% 单核半屏深度镜像：工作核约快 1.09 ms，但 2.01 ms 回写反超；当时的逐目标像素位测试合成也变慢，因此该“镜像+回写”实现已否决。
- [x] RX-78 compact solid panel 将可信深度验证和着色从两个三角形提升到面板级；真机 65% `project+raster` 67.551→64.336 ms，48 姿态整屏哈希一致，已接受。
- [x] RX-78 65% 下半屏颜色带优先分配 74.4 KiB 片内 RAM；颜色生成后直接被合成消费、无需回写。两次 48 姿态 A/B 均逐像素一致，完整周期约 110.59→107.75 ms（9.04→9.28 FPS）；分配失败自动回退 PSRAM。
- [x] 将 `project_raster_us` 拆为 panel prepare、space wait、CPU0/CPU1 raster；65% 实测约 19.606/0.003/42.879/44.542 ms，确认双核光栅平衡且顶点/面片准备仍是独立大项。
- [x] RX-78 直接从共享顶点缓存生成紧凑纯色面片，跳过通用 UV/material 中间表示；两次 48 姿态 A/B 均逐像素一致，panel prepare 20.199→14.419 ms，完整周期 110.430→102.614 ms（9.06→9.74 FPS）。
- [x] 将 16 KiB 共享顶点 `ready` 状态表迁入片内 RAM；两次 48 姿态 A/B 均逐像素一致，直接阶段约 59.533→59.423 ms，端到端周期 105.221→102.908 ms；差值中只有 0.11 ms 已被直接归因，其余作为系统效应记录，分配失败自动回退。
- [x] 拆出导航 `overlay_us`；原生 framebuffer 线段 A/B 虽 48 姿态哈希一致，但 overlay 仅 122→96 µs、draw 仅改善 28 µs，已回退重复线段内核。
- [x] 增加按实际顶点数分配的片内 scratch buffer，RX-78 仅迁移 2,674 个投影坐标（32,088 B）；48 姿态哈希一致，panel prepare 14.273→13.554 ms，draw 87.123→86.391 ms，分配失败自动回退 PSRAM。
- [x] 65% native composite 改为按已有占用位图置位源像素驱动最近邻写出；`blit` 15.023→9.383 ms，完整周期约 108.2 ms / 9.24 FPS，48 姿态整屏哈希一致。
- [x] 在 sparse composite 不再读取深度后，重新实现 65% 下半屏片内深度为权威存储且不做 PSRAM 回写；与 32,088 B 片内投影前缀同时驻留。48 姿态哈希一致，depth clear 19.442→17.507 ms、draw 88.254→86.472 ms，内部 RAM 仍余 55,919 B；跨带单核回退和分配失败路径保持安全。
- [x] 验证逐行首末占用范围的连续深度清理；96+96 帧哈希一致，depth clear 4.563→1.864 ms，但边界记录使 raster 增加 0.773 ms，draw 净收益仅 0.105 ms 且增加约 1.7 KiB 状态，已回退。
- [x] 改为在清理阶段直接从已有占用位图推导每行首末置位，不增加光栅期记录；两次独立重启、每次 96+96 帧哈希一致，depth clear 稳定约 4.26→2.47 ms、draw 约 85.84→85.28 ms，已接受。
- [x] 验证将纯色面片光照颜色从两个光栅核前移到串行准备阶段；96+96 帧哈希一致但 draw 84.941→84.974 ms，已回退。
- [x] 验证把 65% 片内权威深度从下半屏向上扩展：98,256 B 连续块受碎片限制无法申请，拆成 76,176+22,080 B 后可用；实际覆盖跨界拆分、窗口位置和 8/16/24/32/40 行带宽均完成真机扫描，全部哈希一致。最佳 40 行方案 depth clear 2.548→2.059 ms，但 draw 85.258→85.289 ms，且内部 RAM 仅余 33,467 B，已完整回退。结论：分段可利用碎片内存，但跨界图元成本和安全余量必须用完整帧 A/B 决策。
- [x] 验证把 ABC/ACD 两三角形合并成保持原重心与覆盖语义的四边形联合扫描；288 连续姿态及 6,720 组回归逐像素一致，但主机 1,245.11→1,287.05 µs。外轮廓联合行区间和逐像素对角线分类成本超过共享 setup 收益，未上真机并已回退；后续只考虑逐行精确区间合并或共享平面深度方案。
- [x] 验证带 Q13 整数边界回退保护的三角形深度平面；6,720 组主机与真机 48 姿态哈希一致。最终以独立模板实例隔离候选、消除关闭路径的热循环分支后，raster 55.674→57.359 ms、draw 85.342→87.028 ms，已回退。A/B reference 必须指令级隔离候选开销。
- [x] 将 `shell()` 端盖相邻扇形三角形按原共享对角线打包为真实四边形；面板 3,288→3,156。跨版本主机 3,456 帧及真机三轮 96 帧整屏哈希一致，panel prepare 14.253→13.929 ms、raster 56.650→56.126 ms、draw 87.257→86.577 ms，已接受。建模阶段应消除固定四边形容器中的退化三角形调用，但不得任意改变三角剖分。
- [x] 将 `tube()` 起点、实心终点和空心内底盖的相邻扇形三角形继续按原对角线打包；面板 3,156→2,828，埋面诊断仍保留原单三角形语义。跨版本主机 3,456 帧及真机三轮 96 帧哈希一致，panel prepare 13.929→12.890 ms、raster 56.126→54.551 ms、draw 86.577→84.663 ms，已接受；两轮建模优化累计约省 2.59 ms draw。
- [x] 将 `cover()` 正面和背面的相邻扇形三角形按原径向对角线打包；面板 2,828→2,744。跨版本主机 3,456 帧及真机三轮 96 帧哈希一致，panel prepare 12.890→12.661 ms、raster 54.551→54.197 ms、draw 84.663→84.194 ms，已接受。
- [x] 为紧凑纯色面片在 `visibility` 高位编码三角形拓扑，结构体大小和 PSRAM 流量不变；共享索引路径在准备阶段直接读取重复索引，通用路径只压紧时比较一次坐标，两个光栅分区不再重复推断。共享赛车测试、6,720 组主机回归及真机三轮 96 帧哈希一致；相对总是发出两组三角形，raster 54.197→53.738 ms、draw 84.194→83.775 ms，已接受。框架应把图元类型保留进紧凑命令流。
- [x] 65% 稀疏合成只在最近邻映射切换源行时解码占用位图和 RGB565，重复目标行复用已有临时数组中的紧凑 `source x + native color` 列表；不增加栈或持久内存。共享赛车测试、6,720 组主机回归和真机四轮 96 帧哈希一致，`blit` 8.199→7.824 ms、draw 83.775→83.375 ms，已接受。与此前失败的完整目标 span 缓存不同，本方案不复制背景、不构造额外 span。
- [x] 验证以误差累加替代最近邻 x/y 映射的整数除法；主机回归和真机三轮 96 帧哈希一致，但 `blit` 7.827→7.829 ms，draw 差异仅约 0.013 ms，已回退。缩放坐标除法不是当前合成瓶颈。
- [x] 将光栅首次覆盖、稀疏深度清理和 sparse composite 共用的 22,472 B 占用位图优先迁入片内 RAM；保留 32 KiB 分配前安全余量，失败自动回退 PSRAM。6,720 组主机回归和真机七轮 96 帧哈希一致，depth clear 3.870→3.247 ms、raster 53.724→53.381 ms、blit 7.824→7.709 ms、draw 83.375→82.489 ms，已接受。小型高频 RMW 元数据应优先于大型顺序数据进入快内存。
- [x] 验证为上下半屏分别建立有序面片索引；单姿态每核遍历量由 2,744 降为 1,130/830，18 轮各 96 帧哈希一致，CPU0/CPU1 光栅约 40.080/40.150→39.62/39.45 ms；但串行写索引使 panel prepare 12.498→约 13.17 ms、draw 82.489→约 82.64 ms，已回退。后续只有在命令生成期可零额外外存写入地产生分带流时重开。
- [x] 验证缓存三角形扫描边的 min/max、水平标志和端点；主机 6,720 组与共享赛车测试通过、真机五轮 96 帧哈希一致，但额外栈工作集/加载使两核光栅各慢约 0.35–0.42 ms，draw 82.489→82.898 ms，已回退。源码运算提升必须同时检查 Xtensa 寄存器压力。
- [x] 扫描保守逐行边界收缩的面积阈值 0–1024，并在 192–448 细扫；全部 96 帧组哈希一致。0/64/128 和 512/1024 均明显慢于 256；三轮 288 平均约 82.864 ms，原 256 四组平均约 82.829 ms，保留原可常量折叠阈值，不引入 RX 专用魔数或运行时成员。
- [x] 隔离编译器优化范围：整个 `museum_renderer.cpp` 使用 `O3` 虽降低准备和光栅时间，却使 `blit` 7.709→8.887 ms 且固件增加约 58 KiB，已回退；仅对 ESP 侧 `MuseumRenderer::render()` 使用 `O3` 后，10×96 帧哈希一致，panel prepare 12.498→10.633 ms、draw 82.489→80.676 ms、周期 94.269→92.473 ms（10.81 FPS），正式固件仅增加 10,208 B，已接受。6,720 组像素回归、共享套件及 ASan/UBSan 均通过。编译优化必须按热阶段隔离并同时检查指令缓存、合成路径和体积。
- [x] 仅对 ESP 侧共享 `triangleImpl()` 模板启用 `O3`；10×96 帧哈希一致，CPU0/CPU1 光栅 40.066/40.161→39.446/39.565 ms，raster 51.521→50.887 ms，周期 92.473→91.793 ms（10.89 FPS），正式固件增加 16,384 B、仍余 131,632 B，已接受为框架级优化。尝试把 O3 收窄成单一显式模板实例虽只增约 1.7 KiB，却因失去 `$isra` 参数特化使 raster 暴涨到约 103.8 ms，已否决；模板常量传播是热路径性能契约。
- [x] 验证光栅模板 `fast-math`：未消除 `__divsf3`，热实例 2,620→3,266 B；3×96 帧哈希发生变化，raster 50.9→约 54.9 ms、draw 80.0→约 84.1 ms，同时更慢且失真，已回退。嵌入式浮点热路径不能把宽松语义当作免费收益。
- [x] 验证将三条扫描边拆成标量并展开逐行循环；6,720 组回归和 ASan/UBSan 通过，同条件真机候选/回退各 6×96 帧哈希一致，但周期仅相差数微秒、无可证明收益，已回退。固定三边循环在 O3 下已被有效优化，不再重开同形态源码展开。
- [x] 在紧凑纯色命令的空闲标志位携带“投影深度已验证”不变量，避免 CPU0/CPU1 对每张面片重复验证四顶点。6,720 组回归和共享赛车套件通过；真机基线 10×96、候选 9×96 哈希一致，raster 51.517→51.166 ms、CPU0/CPU1 39.602/40.063→39.184/39.706 ms、draw 81.009→80.649 ms、周期 92.940→92.562 ms，已接受为命令流级优化。
- [x] 仅对 ESP 侧高频 `preparedSolidPanelRows()` 命令回放入口启用 O3；12×96 帧哈希一致，CPU0/CPU1 39.184/39.706→38.838/39.405 ms、raster 51.166→50.833 ms、draw 80.649→80.301 ms、周期 92.562→92.238 ms。符号由约 950 B 增至 5.1 KiB、固件约增加 21.8 KiB，但收益跨批次稳定，已接受；外层命令解码也应按热度独立选择优化级别。
- [x] 验证“串行共享顶点预投影 + 双核命令组装”；10×96 帧哈希一致，但索引整理和分离投影使 panel prepare 10.931→约 13.78 ms、周期 92.238→约 95.0 ms，已回退。双核化前必须先消除新的串行 staging，不能只看两个组装任务的均衡。
- [x] 将 RX-78 V-fin 两端的共面四边三角扇按原径向对角线打包，面板 2,744→2,736；27 个结构视图、6,720 组主机回归和真机全帧哈希一致，周期约 92.238→92.16 ms。头顶和盾牌扇面因不共面会被构造器安全拆回，不强行合并。
- [x] 复用 16 KiB 片内共享顶点 `ready` 表的闲置尾部保存 face pass 标记；10 组交替 A/B 哈希一致，draw 80.264→80.072 ms、周期 92.201→92.003 ms，无新增分配。
- [x] 在同一片内尾部建立上下半屏有序面板索引，重新验证先前因 PSRAM 索引写入而否决的分带路线；10+ 组交替 A/B 哈希一致，结合“已选带”命令标志跳过重复纵向 bounds 后，draw 约 80.15→79.49 ms、周期约 92.08→91.39 ms。分带是否成立取决于索引元数据所在存储层级。
- [x] 验证压缩/对齐紧凑命令：把 `top/bottom` 保守量化为 `int16_t` 可将 60 B 降到 56 B，但同条件周期 91.81→93.90 ms；反向按 32 B burst 对齐到 64 B 更慢至约 95.3 ms。两者均回退，ESP32-S3 外存命令不能只按字节数或单条 burst 数推断收益。
- [x] 为面剔除增加连续 anchor 流，保持原 `dot(normal, eye-point0)` 运算顺序；6,720 组逐像素回归和真机哈希一致。严格控制组证明不是单纯对象填充：cull 约 5.50→3.54 ms，虽使后续 panel prepare 约 11.05→11.45 ms，draw 仍约 79.96→78.32 ms、周期约 91.77→90.14 ms。框架应把高频粗筛字段从大图元记录中拆成紧凑只读流。
- [x] 验证逐 scanline 复用单一 Q13 深度的机会：跨完整回归，首尾同深度的行约占 43.8%，但只覆盖约 21.7% 像素且多为极短 span；整三角形同 Q13 深度仅覆盖约 0.09% 像素。为它增加端点计算和热循环分支不具备收益基础，未进入产品或真机候选。
- [x] 为 prepared solid 面板建立一次性的光栅目标上下文，两个三角形共享已经选择好的颜色/深度平面及 split offset；覆盖、重心、Q13 深度、提交顺序均不变。6,720 组主机回归逐像素一致；真机四组交替 96 姿态 A/B 哈希一致，双核 raster 50.679→49.264 ms、draw 78.163→76.685 ms、完整周期 90.097→88.601 ms。去掉诊断分支后的五组周期稳定在 88.55–88.81 ms（约 11.28 FPS）。框架应把存储层级选择提升到复合图元/命令级，而不是在每个子三角形重复解析。
- [x] 验证把连续 normal/anchor 两条流交错为 24 B 面记录；Mesh 总字节不变、回归和真机哈希一致，cull 约 3.55→3.31 ms，但 panel prepare 与 raster 合计回吐约 0.22 ms，九组周期仅约 88.55 ms，相对 88.68 ms 基线的 0.13 ms 不足以覆盖独立重启噪声和全模型 API 重排成本，已回退。
- [x] 验证将 `facing<-.035` 提前到 `twoSided` PSRAM 读取之前以利用短路；6,720 组和真机哈希一致，但五组周期均值约 88.67 ms、cull 仍约 3.53 ms，与基线完全重合。O3 已能等价调度这些非易失只读访问，源码条件换序已回退。
- [x] 验证在 `solidPanel()` 中把四个共享顶点索引缓存到局部数组、用首顶点初始化 bounds 并省略完全覆盖的局部命令清零；像素回归通过，但真机 panel prepare 约 11.52→11.85 ms、raster 约 49.27→49.62 ms、周期约 88.68→88.99 ms。局部数组和寄存器压力超过少量 PSRAM 重读收益，已回退。
- [x] 为 prepared solid 专用三角形实例引入只含 `x/y/depth` 的 12 B 顶点 ABI，通用贴图路径继续使用 20 B 顶点；6,720 组逐像素回归通过。同一采样器按候选→回退→候选独立重启，17 组候选均值 88.337 ms、9 组基线 88.662 ms，raster 约 48.91 vs 49.27 ms，端到端净省 0.324 ms，整屏哈希逐批一致，固件还减少 672 B，已接受。
- [x] 验证 prepared target 每行预先形成 depth/color 行指针；主机像素一致，但真机 raster 约 48.91→49.93 ms、周期约 88.34→89.36 ms，额外行指针的寄存器压力高于重复 band offset 算术，已回退。
- [x] 验证只对扫描边界做逐行增量、保留最终重心覆盖与深度数学；6,720 组逐像素一致，但 6 组真机 raster 约 49.96 ms、周期约 89.33 ms。新增 edge min/max/x/flat 状态增加栈和寄存器压力，已回退。
- [x] 验证 prepared solid 内部 span 的增量深度；主机首轮即在 RX-78 65% 视图发现 2 个最终遮挡像素变化，未进入真机，已回退。Q13 深度和等深顺序仍是不可放宽的正确性边界。
- [x] 验证只在每行首像素精确计算重心坐标、行内递推 `s/t`；6,720 组主机和真机 96 姿态哈希一致，但 7 组设备周期均值约 88.80 ms，相对 88.34 ms 基线更慢，已回退。主机约 13% 的热段收益不能替代 Xtensa 寄存器压力实测。
- [x] 验证可信纯色 span 一次性范围置位占用位图；语义上与逐像素首次覆盖一致，6,720 组主机及真机哈希通过，但大量短 span 的边界处理使 raster 约 48.91→51.15 ms、周期约 88.34→90.58 ms，已回退。
- [x] 将 65% RX-78 稳定成立的“稀疏占用记录开启”不变量从像素循环提升为面板级模板选择；同一采样器候选/回退各 9×96 帧哈希一致，raster 48.906→47.691 ms、周期 88.332→87.030 ms（11.32→11.49 FPS），已接受。运行时策略应在命令边界选择专用内核，不应留在每像素分支中。
- [x] 在稀疏记录专用实例中将“仅首次覆盖置位”改成幂等无条件 OR；15×96 帧哈希一致，额外片内 RMW 小于不规则 `oldDepth==0` 分支成本，raster 47.691→46.985 ms、周期 87.030→86.351 ms，已接受。
- [x] 在共享顶点索引阶段建立模型水平半径与 Y 包络，用相机 pivot 证明整帧远离近裁剪面；RX-78 全姿态/装备最坏深度仍大于 4.4。可信实例删除每顶点近面分支和每角 `z==0` 回退检查，9×96 帧哈希一致，panel prepare 11.531→10.677 ms、周期 86.351→85.485 ms（约 11.70 FPS），已接受；包络不成立时自动走通用裁剪路径。

完成标准：条带化不仅降低内存，还在真机上降低 `project_raster_us` 或明确证明该路线无收益。

## 6. P3：屏幕清理与提交策略

该项与条带光栅分开验证，避免无法归因。

- [ ] 量化 468×466 整屏提交、424×424 模型区域提交和多矩形提交的单独耗时。
- [ ] 确认 AMOLED framebuffer 驱动的 clip 是否真正减少总线像素传输，不只依据 API 语义推断。
- [ ] 记录上一帧和当前帧的模型投影包围盒，评估二者并集实际覆盖率。
- [ ] 对房间网格线记录新旧线段 dirty rect，评估合并后是否仍接近整屏。
- [ ] 比较单个大 dirty rect 与少量多 rect 提交，包含地址窗口和事务启停开销。
- [ ] 如 dirty 合并面积持续超过全屏的 80%，停止继续复杂化提交系统。
- [x] 用旧房间线段擦除 + 上一帧模型占用位图验证稀疏 framebuffer 恢复；零散写显著慢于连续整屏填充，已否决。
- [x] 在满足原生 RGB565、rotation 0、偶数宽度、4-byte 对齐与 stride guard 时使用 32-bit 成对连续清屏；收益很小但稳定且整屏哈希一致。
- [x] 验证 CPU0/CPU1 各清一半完整 framebuffer；哈希一致，但 80 MHz PSRAM 顺序写带宽已经饱和，周期最多仅改善约 0.1–0.2 ms，复杂度不值得保留，已回退。
- [x] 将旧网格擦除进一步改成复刻 M5GFX Bresenham 规则的原生 framebuffer 直写；哈希仍为 `1794963570:3828763871`，但稀疏模型恢复加网格擦除的 background 仍约 14.3 ms，高于整屏连续清理约 12.3–12.7 ms，周期约 90.6 ms，整条稀疏 framebuffer 清理路线已回退。
- [ ] 不通过固定房间或删除网格来制造性能收益，除非将其明确列为视觉方案变更。

预期：仅将整屏缩到 424×424 的最大面积收益约 18%，实际收益还要扣除房间线和多矩形事务开销。

## 7. P4：可选质量/帧率策略

只有在 P1–P3 后仍不能达到跟手目标时再评估。

- [ ] 测试 55%/60%/65%/75% 拖动采样的帧时与视觉差异。
- [ ] 如做自适应质量，使用慢滞环和连续超预算窗口，避免档位抖动。
- [ ] 拖动中降档，松手后必须有一帧精确的 100% 恢复图。
- [ ] 分别验证 RX-78 实体着色和 Nu 隐藏线，不共用未经评审的质量阈值。
- [ ] 几何 LOD 作为最后手段，必须独立视觉验收，不与无损优化混在同一个 A/B 中。

## 8. 回归与验收矩阵

### 8.1 主机正确性

- [x] 运行 `tools/test_gundam_museum.sh`。
- [x] 运行 `tools/test_gundam_museum_perf.sh`。
- [x] 使用 `SANITIZE=1` 重跑当前几何与性能基线；待 strip 缓冲/bin 落地后还需再跑其边界用例。
- [ ] 覆盖 RX-78/Nu、65%/100%、完整装备、正斜侧后、俯仰上下限。
- [ ] 覆盖近裁剪压力、极薄面、屏外包围盒和条带边界。
- [ ] 使用固定种子的连续随机 yaw/pitch，不只测规则角度网格。
- [ ] 在仍以逐像素一致为目标时，比较完整 framebuffer，不只比较模型包围盒。
- [ ] 产生并实际打开拼图检查，至少查看轮廓、天线、眼部、背包、枪盾遮挡和 Nu 隐藏线。

### 8.2 真机验收

- [ ] 同一台设备、同一构建配置、同一模型和同一输入脚本进行基线/候选 A/B。
- [ ] 报告平均帧率、P95 帧时、峰值帧时和持续超预算次数。
- [ ] 单独报告 `draw_us` 和 `present_us`，避免把屏幕提交收益归因到光栅。
- [ ] 确认触摸旋转无明显排队、跳角或松手后延迟恢复。
- [ ] 持续旋转后检查栈余量、堆余量、退出回收和再次进入。
- [ ] 检查圆屏边缘、条带接缝、闪烁、残影和部分帧提交。

## 9. 决策门槛

### 继续条带路线

同时满足：

- 正确性回归通过；
- 工作区和 PSRAM 压力显著下降；
- 真机 `project_raster_us` 或整帧时有稳定改善；
- 没有将负载转移成更大的 `blit_us`/`present_us`。

### 停止条带路线

任一成立：

- 真机帧时无可重复改善；
- 面片重复处理抵消了 internal RAM 收益；
- Nu 隐藏线无法在合理复杂度内保持正确；
- internal RAM 分配破坏任务栈、驱动或其他 App 的安全余量；
- 性能收益仅来自不可接受的画质降级。

### 转向显示提交优化

如 `present_us` 已经是整帧最大阶段，或条带化后成为最大阶段，暂停光栅微优化，优先验证 dirty rect 和驱动提交开销。

## 10. 每轮证据清单

每个候选至少保留：

- commit 和固件 SHA-256；
- 测试命令与退出码；
- ASan/UBSan 结果；
- 逐像素比较结果；
- RX-78/Nu 关键视角拼图；
- 真机原始日志；
- 解析后的 draw/present/阶段/FPS 表；
- 内存、栈和退出回收数据；
- 候选与基线的明确结论，包括被否决的试验。

## 11. 建议的首个实验

先不改产品默认路径，添加可编译切换的 Museum `424×32` strip 原型，并保留当前 `424×424` 光栅器作为 reference。原型首轮只回答三个问题：

1. 是否能在 RX-78 和 Nu 上保持完整 framebuffer 逐像素一致；
2. 是否能将颜色/深度工作集稳定放入 internal RAM；
3. 是否在真机稳态拖动中降低 `project_raster_us` 和整帧时。

这三项确认后，再决定是否投入多 dirty rect、自适应质量或更深的光栅内循环改写。
